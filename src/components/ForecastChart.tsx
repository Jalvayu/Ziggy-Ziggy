import { ResponsiveContainer, LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from 'recharts';
import { ForecastPoint, WeatherVariable } from '../types/weather';
export default function ForecastChart({data,variable}:{data:ForecastPoint[];variable:WeatherVariable}){const key=variable==='temperature'?'temperature':variable==='rainfall'?'rainfall':variable==='wind'?'wind_speed':'apparent_temperature';const anomaly=variable==='temperature'?'temperature_anomaly':variable==='rainfall'?'rainfall_anomaly':variable==='wind'?'wind_anomaly':'heat_anomaly';const chart=data.map((p)=>({...p,baseline:key==='temperature'?35:key==='rainfall'?55:key==='wind'?30:38}));return <div className="chart-box"><div className="chart-head"><div><div className="eyebrow">TREND COMPARISON</div><h2>Forecast vs historical baseline</h2></div><span className="unit-tag">{variable.toUpperCase()}</span></div><ResponsiveContainer width="100%" height={255}><LineChart data={chart}><CartesianGrid strokeDasharray="3 3" stroke="#243142"/><XAxis dataKey="label" stroke="#7f8da2"/><YAxis stroke="#7f8da2"/><Tooltip contentStyle={{background:'#101a27',border:'1px solid #29394d',borderRadius:8}}/><Legend/><Line type="monotone" dataKey={key} name="Forecast" stroke="#65d7c5" strokeWidth={3} dot={{r:4}}/><Line type="monotone" dataKey="baseline" name="Historical baseline" stroke="#748197" strokeDasharray="5 5"/><Line type="monotone" dataKey={anomaly} name="Anomaly" stroke="#ff9b69" strokeWidth={2}/></LineChart></ResponsiveContainer></div>}
