import { BrainCircuit, ShieldCheck } from 'lucide-react';
import { AnomalyData } from '../types/weather';
export default function AIInsight({data}:{data:AnomalyData}){return <section className="panel ai-panel"><div className="panel-title"><div className="title-icon"><BrainCircuit size={19}/></div><div><div className="eyebrow">MODEL OUTPUT · ISOLATION FOREST</div><h2>AI Weather Intelligence</h2></div><span className="risk-badge extreme">{data.risk_level}</span></div><p className="insight-copy">{data.explanation}</p><div className="model-grid"><div><span>Anomaly score</span><strong>{data.isolation_forest_score.toFixed(2)}</strong></div><div><span>Confidence</span><strong>{Math.round(data.confidence*100)}%</strong></div><div><span>Signals detected</span><strong>{data.detected_variables.length}</strong></div></div><div className="confidence"><div><span>Model confidence</span><b>{Math.round(data.confidence*100)}%</b></div><div className="bar"><i style={{width:`${data.confidence*100}%`}}/></div></div><div className="explain"><ShieldCheck size={16}/> Human-readable explanation generated from mock inference</div></section>}
