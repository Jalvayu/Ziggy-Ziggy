import { Pause, Play, SkipBack, SkipForward } from 'lucide-react';
export default function TimeSlider({index,setIndex,playing,setPlaying,labels}:{index:number;setIndex:(n:number)=>void;playing:boolean;setPlaying:(v:boolean)=>void;labels:string[]}) { return <section className="timeline-panel"><div className="section-heading"><div><div className="eyebrow">TEMPORAL ANALYSIS</div><h2>Spatio-Temporal Time Machine</h2></div><span className="step-label">{labels[index]} forecast step</span></div><div className="timeline"><button className="icon-btn" onClick={()=>setIndex(Math.max(0,index-1))} aria-label="Previous forecast step"><SkipBack size={17}/></button><input type="range" min="0" max={labels.length-1} value={index} onChange={e=>setIndex(Number(e.target.value))}/><button className="icon-btn" onClick={()=>setIndex(Math.min(labels.length-1,index+1))} aria-label="Next forecast step"><SkipForward size={17}/></button><button className="play-btn" onClick={()=>setPlaying(!playing)}>{playing?<Pause size={16}/>:<Play size={16}/>} {playing?'Pause':'Play forecast'}</button></div><div className="ticks">{labels.map((l,i)=><button className={i===index?'selected':''} onClick={()=>setIndex(i)} key={l}>{l}</button>)}</div></section>; }
