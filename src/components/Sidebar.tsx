import { MapPin, Clock3, Layers3 } from 'lucide-react';
interface Props { variable:string; setVariable:(v:any)=>void; location:string; setLocation:(v:string)=>void; duration:number; setDuration:(v:number)=>void; }
export default function Sidebar({variable,setVariable,location,setLocation,duration,setDuration}:Props) { return <aside className="sidebar"><div className="eyebrow">CONTROL CENTER</div><h2>Forecast layers</h2><label>Weather variable</label><div className="segmented">{[['temperature','Temperature'],['rainfall','Rainfall'],['wind','Wind'],['heat','Heat']].map(([v,l])=><button className={variable===v?'active':''} onClick={()=>setVariable(v)} key={v}>{l}</button>)}</div><label><MapPin size={14}/> Region / location</label><select value={location} onChange={e=>setLocation(e.target.value)}><option>New Delhi</option><option>Mumbai</option><option>Kolkata</option><option>Hyderabad</option><option>Jaipur</option></select><label><Clock3 size={14}/> Forecast horizon</label><div className="horizon">{[24,48,72,96,120].map(h=><button className={duration===h?'active':''} onClick={()=>setDuration(h)} key={h}>{h}h</button>)}</div><label><Layers3 size={14}/> Visualization mode</label><select defaultValue="anomaly"><option value="anomaly">Anomaly intensity</option><option>Forecast value</option><option>Risk contours</option></select><div className="prototype-note"><span className="dot"/><div><strong>Prototype data</strong><p>Simulated forecast stream. API-ready architecture.</p></div></div></aside>; }
